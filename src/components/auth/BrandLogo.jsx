export default function BrandLogo({ className = '' }) {
  return (
    <span
      className={`font-sans text-2xl font-semibold tracking-tight text-brand-text ${className}`}
    >
      PetroFlow
    </span>
  )
}
